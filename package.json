{
	"name": "schulbert",
	"private": true,
	"version": "0.0.1",
	"type": "module",
	"scripts": {
		"bro": "BROWSER=firefox vite dev --open",
		"dev": "vite dev",
		"git": "git add . && git commit -am ok && git push origin frontend",
		"build": "vite build",
		"postbuild": "rsync -avz --delete --chmod=D775,F664 build/ music:/www/data/schulbert/",
		"check": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json"
	},
	"devDependencies": {
		"@sveltejs/adapter-auto": "^7.0.1",
		"@sveltejs/kit": "^2.63.0",
		"@sveltejs/vite-plugin-svelte": "^7.1.2",
		"svelte": "^5.56.1",
		"svelte-check": "^4.6.0",
		"typescript": "^6.0.3",
		"vite": "^8.0.16"
	},
	"dependencies": {
		"@sveltejs/adapter-static": "^3.0.10"
	}
}
